Read ~/Documents/antigravity/marketing-agent/src/cli.py (the video command at lines 132-209) and ~/Documents/antigravity/marketing-agent/tests/test_video_converter.py (tests from phase 1). Create ~/Documents/antigravity/marketing-agent/tests/test_cli_video.py with 3-4 pytest tests for the video CLI command. The video command uses Typer, calls run_pipeline then angle_to_video_data then saves a JSON file. Test 1 (mock success): use unittest.mock.patch to mock "src.cli.run_pipeline" to return a mock AgentOutput with 1 TrendWithAngles containing 2 mock ContentAngles with brand_alignment_score=0.9, mock "src.cli.angle_to_video_data" to return {"id": "fahad-ep-test", "title": "Test", "pillar": "ai", "scenes": []}, invoke the typer app using typer.testing.CliRunner().invoke(app, ["video", "--mock", "--output", "/tmp/test_video_out"]), assert result.exit_code == 0 and a file /tmp/test_video_out/fahad-ep-test.json was created. Test 2 (pick flag): same mocks but invoke with ["video", "--mock", "--pick", "2", "--output", "/tmp/test_video_out2"], verify that angle_to_video_data was called with the second ContentAngle (index 1). Test 3 (no angles): mock run_pipeline to return AgentOutput with results=[], invoke with ["video", "--mock"], assert result.exit_code == 1. After writing tests run "uv run pytest tests/ -v" to run all tests and verify all pass including phase 1 tests. Run "uv run ruff check src/ tests/" for lint. Then do a real E2E: run "uv run python -m src.cli video --mock" from ~/Documents/antigravity/marketing-agent/ and verify a JSON file appears in output/videos/ with valid VideoData structure by running "cat output/videos/*.json | python -m json.tool". IMPORTANT GIT SAFETY: never use git add -A. Run "git diff --cached --name-only | grep -E '\.env|\.key|\.pem|secret|password'" before committing to check for secrets. Stage with "git add tests/" then commit with "git commit -m 'test: add video converter and CLI tests'". Target: 3+ new tests pass, all phase 1 tests still pass, E2E mock produces valid JSON, zero ruff errors.
