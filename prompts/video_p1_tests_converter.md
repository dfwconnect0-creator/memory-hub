Read ~/Documents/antigravity/marketing-agent/src/video_converter.py, ~/Documents/antigravity/marketing-agent/src/models.py, and ~/Documents/antigravity/marketing-agent/src/ai_orchestrator.py. The video converter is already fully built — your job is to add tests only, do NOT change the source code. Create ~/Documents/antigravity/marketing-agent/tests/__init__.py (empty). Create ~/Documents/antigravity/marketing-agent/tests/test_video_converter.py with 8 tests using pytest and unittest.mock. Test _make_topic_slug: call it with Arabic text containing "الذكاء الاصطناعي" and assert result is "ai", call with "AI jobs Saudi" and assert result contains "ai" or "jobs", call with "hello world" and assert result is "hello-world". Test _parse_video_response: pass it a clean JSON string with id, title, pillar, scenes keys and assert the result is a dict with those keys, pass it markdown-fenced JSON (wrapped in triple backticks with json lang) and assert it still parses correctly, pass it garbage text "not json at all!!!" and assert it raises ValueError. Test _mock_video_data: create a ContentAngle and Trend using the models from src/models.py (set headline="Test", hook="Test hook", and for Trend set title="AI testing", source=TrendSource.GOOGLE_TRENDS), call _mock_video_data(angle, trend) and assert the result dict has keys "id", "title", "pillar", "scenes", assert scenes is a list with at least 3 items, assert scenes[0]["type"] == "hook" and scenes[-1]["type"] == "cta". Test angle_to_video_data: use asyncio.run() to call it with mock=True and assert it returns a dict without making any API call, then test with mock=False by using unittest.mock.patch on "src.video_converter.generate_content" to return a valid JSON string matching the VideoData shape and assert it calls generate_content once and returns a parsed dict with a scenes key. Run "uv run pytest tests/test_video_converter.py -v" then "uv run ruff check src/ tests/" to verify. Target: 8 tests pass, zero ruff errors.
